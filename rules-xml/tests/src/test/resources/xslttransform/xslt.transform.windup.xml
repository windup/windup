<?xml version="1.0"?>
<ruleset xmlns="http://windup.jboss.org/schema/jboss-ruleset" id="xslt_transform_rules">
    <rules>
        <rule>
            <when>
                <xmlfile matches="/rootTag" in="{*}">
                </xmlfile>
            </when>
            <perform>
                <xslt title="XSLT Transformed Output" extension="-test-result.html" template="xsl-with-include.xsl"/>
            </perform>
        </rule>
    </rules>
</ruleset>
